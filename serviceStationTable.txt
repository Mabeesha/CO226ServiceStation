create table serviceStationInfo
    (station_id INT NOT NULL AUTO_INCREMENT,
    station_name VARCHAR(25),
    station_location VARCHAR(50),
    station_specs ENUM ('All','Car','Van','Jeep'),
    station_servicesProvided ENUM('Cut & Polish','Interior Cleaning','Light Vehicle Service','Heavy Vehicle Service','Vehicle Wash'),
    station_state ENUM ('Free','Busy'),
    station_gps_latitude float(10,6),
    station_gps_longitude float(10,6),
    contact_num INT,
    user_name VARCHAR(25),
    acc_password VARCHAR(8),
    PRIMARY KEY(station_id));



create table serviceStationInfo
    (station_id INT NOT NULL AUTO_INCREMENT,
    station_name VARCHAR(25),
    station_location VARCHAR(50),
    station_specs ENUM ('All','Car','Van','Jeep'),
    
    station_state ENUM ('Free','Busy'),
    
    contact_num INT,
    user_name VARCHAR(25),
    acc_password VARCHAR(8),
    PRIMARY KEY(station_id));
    
    
    
create table serviceStationInfo
    (station_id INT NOT NULL AUTO_INCREMENT,
    station_name VARCHAR(25),
    station_location VARCHAR(50),
    station_specs ENUM ('All','Light Vehicles','Heavy Vehicles'),
    
    station_state ENUM ('Free','Busy'),
    
    contact_num INT,
    user_name VARCHAR(25),
    acc_password VARCHAR(8),
    PRIMARY KEY(station_id));

INSERT INTO serviceStationInfo VALUES ('001','Car Clean','Dangolla,Kandy','All','Free','075235','user1','123');

INSERT INTO serviceStationInfo VALUES ('002','ReadyWash','Panideniya,Kandy','All','Busy','071235','user2','456');
